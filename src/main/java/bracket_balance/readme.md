Crear una función que determine si los paréntesis, corchetes y llaves están balanceados.

Por ejemplo:

    "[{()}]"      returns true  
    "]["          returns false
    "(a+b-[c-d])" returns true
    "([)]"        returns false
