Desarrollar una función que determine si el texto enviado como primer parámetro termina con el texto enviado como segundo parámetro.

Por ejemplo:

    solution('abc', 'bc') // returns true
    solution('abc', 'd') // returns false

